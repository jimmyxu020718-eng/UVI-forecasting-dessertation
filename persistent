# persistence model
def model_persestence(x):
    return x

predictions = list()
persistence_series = extended_series.shift(24)

for x in test_X:
    yhat = model_persistence(x)
    predictions.append(yhat)
test_score = mean_squared_error(test_y, predictions)
